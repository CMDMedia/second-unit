# Custom workflows

This preview does not bundle generation presets. Use LOAD WORKFLOW in the panel to import a compatible ComfyUI API-format JSON file. Required models and nodes must already be installed on your ComfyUI server. Imported workflows are marked custom; a compatibility check does not certify their visual quality.
